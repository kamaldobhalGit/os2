import "./sideNav.css"; // Import the CSS file for side navbar

export default function SideNav() {
  return (
    <div className="side-navbar">
      {/* Add your side navigation content here */}
    </div>
  );
}
